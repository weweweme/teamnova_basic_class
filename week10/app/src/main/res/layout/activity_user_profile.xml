<?xml version="1.0" encoding="utf-8"?>
<!-- 유저 프로필 화면 (다른 유저를 눌러 진입) -->
<!-- 위: 프로필 헤더(아바타·별명·소개·통계·팔로우), 아래: 그 유저가 작성한 리뷰 목록 -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".community.UserProfileActivity">

    <!-- ════════ 프로필 헤더 ════════ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <!-- 아바타 (색은 코드에서 tint) -->
        <TextView
            android:id="@+id/textViewProfileAvatar"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_avatar_circle"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:textSize="28sp"
            android:textStyle="bold"
            tools:text="픽" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textViewProfileNickname"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="20sp"
                android:textStyle="bold"
                tools:text="픽셀덕후" />

            <TextView
                android:id="@+id/textViewProfileBio"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="#888888"
                android:textSize="13sp"
                tools:text="인디부터 AAA까지 잡식" />

            <TextView
                android:id="@+id/textViewProfileStats"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:textColor="#666666"
                android:textSize="13sp"
                tools:text="리뷰 3 · 팔로워 2 · 팔로잉 1" />
        </LinearLayout>

        <!-- 팔로우/팔로잉 버튼 (내 프로필이면 코드에서 숨김) -->
        <Button
            android:id="@+id/buttonProfileFollow"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:textSize="13sp"
            tools:text="팔로우" />
    </LinearLayout>

    <!-- 구분선 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#22000000" />

    <!-- 작성한 리뷰 제목 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="4dp"
        android:text="@string/user_profile_reviews_title"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- 그 유저가 작성한 리뷰 목록 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerProfileReviews"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        tools:listitem="@layout/item_review_feed" />

    <!-- 리뷰가 없을 때 안내 -->
    <TextView
        android:id="@+id/textViewProfileNoReviews"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/user_profile_no_reviews"
        android:textColor="#888888"
        android:visibility="gone" />

</LinearLayout>
