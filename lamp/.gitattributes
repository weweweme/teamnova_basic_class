# 셸 스크립트는 반드시 LF(유닉스 줄바꿈)로 유지한다.
#   윈도우 Git은 기본적으로 받을 때 줄바꿈을 CRLF로 바꾸는데,
#   그러면 리눅스 컨테이너 안 bash가 줄 끝의 \r 까지 명령으로 읽어서
#   "$'\r': command not found" 같은 오류를 내며 컨테이너가 안 뜬다.
*.sh text eol=lf

# Apache 설정도 같은 이유로 LF 고정
*.conf text eol=lf
