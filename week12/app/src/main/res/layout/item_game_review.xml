<?xml version="1.0" encoding="utf-8"?>
<!-- "다른 사람들의 평가" 목록의 리뷰 한 개 -->
<!-- 작성자 아바타(색+첫 글자) + 별명 + 별점 + 한줄평 -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- 작성자 아바타 (색은 코드에서 backgroundTintList로 칠함) -->
    <TextView
        android:id="@+id/textViewReviewerAvatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_avatar_circle"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        tools:text="픽" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <!-- 별명 + 별점 (한 줄) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/textViewReviewerNickname"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="15sp"
                android:textStyle="bold"
                tools:text="픽셀덕후" />

            <TextView
                android:id="@+id/textViewReviewerRating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#EF6C00"
                android:textSize="14sp"
                tools:text="★ 4.5" />
        </LinearLayout>

        <!-- 한줄평 -->
        <TextView
            android:id="@+id/textViewReviewerReview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#555555"
            android:textSize="14sp"
            tools:text="엘든 링 어렵지만 결국 명작" />

        <!-- 좋아요(하트) — 누르면 토글, 색은 코드에서 (누름=빨강/안누름=회색) -->
        <TextView
            android:id="@+id/textViewReviewLike"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="2dp"
            android:paddingStart="8dp"
            android:paddingEnd="4dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:textSize="13sp"
            tools:text="♥ 3" />
    </LinearLayout>

</LinearLayout>
